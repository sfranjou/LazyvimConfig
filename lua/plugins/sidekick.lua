return {
  "folke/sidekick.nvim",
  opts = {
    -- Disable Next Edit Suggestions (in-editor AI suggestions)
    -- Chat window (CLI integration) remains available
    nes = {
      enabled = false,
    },
  },
}
